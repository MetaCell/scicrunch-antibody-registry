# Environment variables for Vite development server
# Copy this file to .env.local and adjust the values as needed

# Development server proxy target
VITE_DOMAIN=http://localhost:8000

# Accounts API domain (optional, will default to VITE_DOMAIN with 'api.accounts' subdomain)
# VITE_ACCOUNTS_API_DOMAIN=http://api.accounts.areg
